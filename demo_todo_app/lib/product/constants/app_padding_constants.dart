import 'package:flutter/cupertino.dart';

class AppPaddingConstants {
  static const EdgeInsets allPadding = EdgeInsets.all(8.0);
  static const EdgeInsets allPadding2x = EdgeInsets.all(16.0);
  static const EdgeInsets zeroPadding = EdgeInsets.zero;
  static const EdgeInsets bottom10Padding = EdgeInsets.only(bottom: 10);
  static const EdgeInsets symmetricPadding =
      EdgeInsets.symmetric(horizontal: 5, vertical: 5);
  static const EdgeInsets symmetricPadding2 =
      EdgeInsets.symmetric(horizontal: 20, vertical: 5);
  static const EdgeInsets horizontal20Padding = EdgeInsets.symmetric(horizontal: 20);
  static const EdgeInsets horizontal8Padding = EdgeInsets.symmetric(horizontal: 8);
  static const EdgeInsets ltbrPadding =
      EdgeInsets.only(left: 10, right: 10, bottom: 12, top: 5);
}
